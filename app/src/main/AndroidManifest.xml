<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Main"
        tools:targetApi="31">
        <activity
            android:name=".Librarian.BookDetails"
            android:exported="false" />
        <activity
            android:name=".Librarian.UpdateBookActivity"
            android:exported="false" />
        <activity
            android:name=".Librarian.AddingBookActivity"
            android:exported="false"
            android:label="Adding Book"
            android:parentActivityName=".Librarian.BookListActivity" />
        <activity
            android:name=".Librarian.BookListActivity"
            android:exported="false" />
        <activity
            android:name=".SplashMain.MainActivity"
            android:exported="true"
            android:theme="@style/splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>